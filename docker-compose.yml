version: "3"
services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib.mysql
    restart: always
    environment:
      .env
  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
    networks:
      - adm-net
networks:
  adm-net:
volumes:
  db_data:
